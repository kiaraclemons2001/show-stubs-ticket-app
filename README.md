# show-stubs-ticket-app
A client server application called Show Stubs that sells ticket to the client. The code was built to be robust with a SQL database (SQLite database file) with SHA-256 encryption. 
